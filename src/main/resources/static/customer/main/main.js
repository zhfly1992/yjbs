/** 页面-js */
layui.define(['layer', 'jquery', 'form', 'front', 'myutil', 'fv'], function(exports){
	let layer = layui.layer,
	$ = layui.jquery,
	form = layui.form,
	fv = layui.fv(),
	U = layui.myutil(),
	kf = layui.front();
	
	/** 页面最高-选择器 */
	let pid = '.company-main';
	
	
});